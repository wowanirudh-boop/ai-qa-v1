[project]
name = "ai-testgen"
version = "0.1.0"
description = "Architecture bootstrap for a chatbot requirements-to-test compiler."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ai-testgen = "ai_testgen.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
